# NanoGPT trace UI

Astro static site for inspecting and comparing the committed NanoGPT optimizer
traces in `../traces/`.

```bash
npm ci
npm run dev
```

`npm run build` generates the production site in `dist/`.
