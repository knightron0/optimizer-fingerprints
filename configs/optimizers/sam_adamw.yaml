name: sam_adamw
family: sam_adamw
hparams:
  lr: 0.001
  betas: [0.9, 0.95]
  weight_decay: 0.01
  eps: 1.0e-8
  rho: 0.05
param_groups:
  all: trainable
metadata:
  geometry: sharpness_aware_neighborhood
  description: SAM perturbation step with AdamW base optimizer over all trainable parameters.
