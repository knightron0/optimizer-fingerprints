name: adafactor
family: adafactor
hparams:
  lr: 0.001
  beta2_decay: -0.8
  eps: [null, 0.001]
  d: 1.0
  weight_decay: 0.01
param_groups:
  all: trainable
metadata:
  geometry: factored_coordinatewise
  description: Adafactor factored coordinatewise updates over all trainable parameters.
