name: adamw
family: adamw
hparams:
  lr: 0.001
  betas: [0.9, 0.95]
  weight_decay: 0.01
  eps: 1.0e-8
param_groups:
  all: trainable
metadata:
  geometry: coordinatewise_adaptive
  description: AdamW over all trainable parameters.
